{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "php artisan serve --host=127.0.0.1 --port=8000",
    "build": "echo \"No JS build step for the API\"",
    "start": "php artisan serve --host=0.0.0.0 --port=8000",
    "lint": "echo \"API lint handled by composer (pint)\"",
    "test": "php artisan test"
  }
}
